import {Container, Row, Col} from 'react-bootstrap'


const Home = (props) => { 
    console.log("this is props", props)
    return(
    <Container>

     </Container > 
     )}
      export default Home